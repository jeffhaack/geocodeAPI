geocodeAPI

A few things need to be done to get the app up and running.  First setup the database
and configure config/database.yml

Run:
rake db:migrate
rake seed_streets
rake thinking_sphinx:index
rake thinking_sphinx:rebuild
rake seed_addresses
